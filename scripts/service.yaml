apiVersion: v1
kind: Service
metadata:
  name: unionblock-web-service
  namespace: unionblock
  labels:
    app: unionblock-web
spec: 
  ports:
    - name: unionblock-web
      port: 3000
  selector:
    app: unionblock-web
